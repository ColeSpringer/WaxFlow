# build artifacts
/bin/
/dist/

# test/profile droppings
*.test
*.out
*.prof

# per-target logs from scripts/fuzz.sh (make fuzz)
/fuzz-logs/

# generated encoder-quality reports (make encoder-quality); nightly artifacts
/quality-report.html
/opus-quality-report.html

# fetched conformance vectors (make verify-vectors); pinned, never committed
/testdata/vectors/

# reference oracle tools built from pinned sources (make opus-tools)
/testdata/tools/

# local scratch config
/waxflow.json

# playwright install for the gated hls.js e2e (make hls-e2e)
/node_modules/
/package.json
/package-lock.json
